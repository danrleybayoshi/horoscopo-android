<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".DetalleHoroscopoActivity">

    <!-- 1. Contenedor Principal del Mensaje y Fondo (clMensajeContainer) -->
    <!-- Este contenedor aloja la imagen de fondo (bola de cristal) y el área de texto. -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/clMensajeContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- 1.1. IMAGEN DE FONDO (BOLA DE CRISTAL) -->
        <ImageView
            android:id="@+id/ivFondoMensaje"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:scaleType="fitXY"
            android:src="@drawable/ic_fortune1"
            android:contentDescription="@string/imagen_fondo_desc"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 1.2. CONTENEDOR DEL MENSAJE (ÁREA DENTRO DE LA BOLA) -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginStart="35dp"
            android:layout_marginEnd="35dp"
            android:layout_marginTop="100dp"
            android:layout_marginBottom="150dp"
            app:cardCornerRadius="200dp"
            app:cardElevation="0dp"
            app:strokeWidth="0dp"
            app:cardBackgroundColor="@android:color/transparent"
            app:layout_constraintBottom_toBottomOf="@id/ivFondoMensaje"
            app:layout_constraintEnd_toEndOf="@id/ivFondoMensaje"
            app:layout_constraintStart_toStartOf="@id/ivFondoMensaje"
            app:layout_constraintTop_toTopOf="@id/ivFondoMensaje">

            <ScrollView
                android:id="@+id/mensajeScrollView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingLeft="28dp"
                android:paddingRight="20dp"
                android:paddingTop="20dp"
                android:paddingBottom="20dp"
                android:scrollbars="none">

                <TextView
                    android:id="@+id/tvMensajeHoroscopo"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@android:color/black"
                    android:textSize="25sp"
                    android:textStyle="italic|bold"
                    android:layout_gravity="center_vertical"
                    tools:text="Cargando horóscopo..." />

            </ScrollView>
        </com.google.android.material.card.MaterialCardView>

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- 2. TÍTULO DEL SIGNO (Flota sobre el fondo) -->
    <TextView
        android:id="@+id/tvTituloSigno"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:textAlignment="center"
        android:textColor="@android:color/white"
        android:textSize="28sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Cáncer (21 Jun - 22 Jul)" />

    <!-- 3. BOTÓN DE FAVORITOS (Flota sobre el fondo - ID requerido por DetalleHoroscopoActivity.kt) -->
    <ImageButton
        android:id="@+id/btnFavoritoDetalle"
        android:layout_width="66dp"
        android:layout_height="66dp"
        android:layout_marginTop="232dp"
        android:layout_marginEnd="38dp"
        android:background="@android:color/transparent"
        android:contentDescription="@string/favorito_desc"
        android:padding="0dp"
        android:scaleType="fitCenter"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_favorite_border"
        app:tint="#D4AF37" />

</androidx.constraintlayout.widget.ConstraintLayout>
